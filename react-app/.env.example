# Copia este archivo como .env.local (dev) o configura la variable en Vercel (prod).
# En producción, apunta a la URL pública del backend en Render, por ej:
#   REACT_APP_API_URL=https://beach-accounting-api.onrender.com
REACT_APP_API_URL=http://localhost:5050
